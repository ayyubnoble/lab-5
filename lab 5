 public static int sumArray(int values[])
   {
      // initialize sum
      int sum = 0;

      // add all elements to sum
      for (int i = 0; i < values.length; i++) {
         sum += values[i];
      }

      // return sum
      return sum;
   }
}
